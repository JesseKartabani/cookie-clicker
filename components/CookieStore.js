import { StyleSheet, Text, View } from "react-native";
import React from "react";

const CookieStore = () => {
  return (
    <View>
      <Text>CookieStore</Text>
    </View>
  );
};

export default CookieStore;

const styles = StyleSheet.create({});
